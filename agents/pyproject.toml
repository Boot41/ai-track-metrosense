[project]
name = "metrosense-agents"
version = "0.1.0"
description = "MetroSense internal ADK agents service"
requires-python = ">=3.12"
dependencies = [
  "google-adk",
  "httpx>=0.27",
]

[tool.uv]
managed = true
