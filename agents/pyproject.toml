[project]
name = "metrosense-agents"
version = "0.1.0"
description = "MetroSense internal ADK agents service"
requires-python = ">=3.12"
dependencies = [
  "google-adk",
  "fastapi>=0.110",
  "httpx>=0.27",
  "uvicorn>=0.29",
]

[tool.uv]
managed = true
